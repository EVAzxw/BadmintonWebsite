<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. 扫描Controller层包（org.example.controller），让Spring识别@Controller注解 -->
    <context:component-scan base-package="org.example.controller"/>

    <!-- 2. 开启Spring MVC注解驱动（支持@RequestMapping、@RequestParam等注解） -->
    <mvc:annotation-driven/>

    <!-- 3. 配置视图解析器（JSP页面的路径和后缀） -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/> <!-- JSP页面存储路径 -->
        <property name="suffix" value=".jsp"/> <!-- JSP页面后缀（如返回"login"对应login.jsp） -->
    </bean>

    <!-- 4. 配置文件上传解析器（处理赛事/装备图片上传，MultipartFile依赖此配置） -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760"/> <!-- 最大上传文件大小：10MB（10*1024*1024） -->
        <property name="defaultEncoding" value="UTF-8"/> <!-- 文件上传编码 -->
    </bean>

    <!-- 5. 配置静态资源映射（让JSP能访问webapp/static下的CSS/JS/图片，避免被DispatcherServlet拦截） -->
    <mvc:resources mapping="/static/css/**" location="/static/css/"/>
    <mvc:resources mapping="/static/js/**" location="/static/js/"/>
    <mvc:resources mapping="/static/images/**" location="/static/images/"/>
</beans>